spring:
  application:
    name: ORDER-SERVICE
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Drivergit gui

    url: jdbc:mysql://localhost:3306/order_service
    username: root
    password: root
    initialization-mode: always
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
test:
  name: "test"
ihor: 25
